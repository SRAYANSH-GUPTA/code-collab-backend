# name: Build And Push Linters

# on:
#   push:
#     branches:
#       - master
#     paths:
#       - "lambda/**"
#       - ".github/workflows/linters-ecr.yml"
#   workflow_dispatch:

# permissions:
#   contents: read

# jobs:
#   build-and-push:
#     name: Push ${{ matrix.language }} linter
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           - language: typescript
#             context: lambda/typescript-linter
#             repository: codecollab-typescript-linter
#           - language: python
#             context: lambda/python-linter
#             repository: codecollab-python-linter
#           - language: dart
#             context: lambda/dart-linter
#             repository: codecollab-dart-linter
#           - language: go
#             context: lambda/go-linter
#             repository: codecollab-go-linter
#           - language: cpp
#             context: lambda/cpp-linter
#             repository: codecollab-cpp-linter

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Check linter build context
#         id: context-check
#         shell: bash
#         run: |
#           if [ -f "${{ matrix.context }}/Dockerfile" ]; then
#             echo "exists=true" >> "$GITHUB_OUTPUT"
#           else
#             echo "exists=false" >> "$GITHUB_OUTPUT"
#           fi

#       - name: Skip missing linter
#         if: steps.context-check.outputs.exists != 'true'
#         run: echo "Skipping ${{ matrix.language }} because ${{ matrix.context }}/Dockerfile does not exist yet."

#       - name: Ensure ECR repository exists
#         if: steps.context-check.outputs.exists == 'true'
#         shell: bash
#         run: |
#           aws ecr describe-repositories \
#             --repository-names "${{ matrix.repository }}" \
#             --region "${{ secrets.AWS_REGION }}" >/dev/null 2>&1 || \
#           aws ecr create-repository \
#             --repository-name "${{ matrix.repository }}" \
#             --image-scanning-configuration scanOnPush=true \
#             --region "${{ secrets.AWS_REGION }}"

#       - name: Build and push image
#         if: steps.context-check.outputs.exists == 'true'
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           ECR_REPOSITORY: ${{ matrix.repository }}
#           IMAGE_TAG: ${{ github.sha }}
#         shell: bash
#         run: |
#           docker build \
#             -t "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
#             -t "$ECR_REGISTRY/$ECR_REPOSITORY:latest" \
#             "${{ matrix.context }}"

#           docker push "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
#           docker push "$ECR_REGISTRY/$ECR_REPOSITORY:latest"

#       - name: Summary
#         if: steps.context-check.outputs.exists == 'true'
#         run: |
#           echo "Pushed ${{ matrix.repository }} with tags:"
#           echo "- latest"
#           echo "- ${{ github.sha }}"
